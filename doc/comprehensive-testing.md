## 0.20.2 caller-root regression

The 1,119/506 assertion counts are unchanged. The archive transport structure guard now verifies that the batch file root is frozen before `shift`, in addition to verifying arbitrary-length option capture.

## 0.20.1 native hotfix

The generated matrix is now 1,119 all-mode assertions / 506 structure
assertions. The added guard requires arbitrary-length argument transport in
`test_all_dumps.bat`; the maintained product-family regression is also pinned to
project 0.20.1.

## 0.19.0 recommended production run

For a clean test + database rebuild + database integrity/query sweep + packaging
run, use the root command:

```bat
all_test_then_all_database_then_test_database_and_all_tools.bat
```

See `full-pipeline.md` for output paths, ZIP/hardlink behavior and performance
logs. The ordinary `test_everything.bat` remains the test-only comprehensive
gate.

For a previously failed 0.16.0/0.16.1 pipeline where all three databases were
already built, 0.19.0 can resume validation and packaging without rebuilding:

```bat
all_test_then_all_database_then_test_database_and_all_tools.bat --resume-built archive-db family-db compact-db
```

Add `--resume-test-results DIR` when the earlier `test-results-*` folder should
also be copied into the new log bundle.



## 0.19.0 structure/layout coverage

The structure scope now expects 495 assertions. In addition to standalone
delivery and prior semantic guards, it verifies the `tools\` relocation, the
modular create/update workflow, database summary launcher, GUI database
discovery contract, and timestamped HTML default. The 478 utility tools are
still exercised through their generated regression scopes; moving them from
the root does not change their behavioral expectations.

# Comprehensive functional, quality, and performance testing

Version 0.14.0 adds one entry point for the recurring optimize/test/retest loop:

```bat
test\test_everything.bat mvs-dumps-root
```

## Default cycle

The default cycle:

1. selects `mvs_2019-03-19` when present (otherwise the first discovered real
   snapshot);
2. runs `test_all.bat` over the complete public regression matrix;
3. analyzes public-test `elapsed_ms` by tool/scope and writes outlier tables;
4. runs `test_fast_archive_sweep.bat` on the three-snapshot synthetic archive;
5. validates a complete full-archive fast plan without performing the long real
   execution.

The default phase-1 plan validation uses the archive sweep's `--quiet-plan`
mode. It still constructs and validates the complete deterministic plan, but
does not print the 79 per-snapshot planning lines. A subsequent production
archive build prints its own planning pass once because that plan is persisted
with the generated database.

The current 0.19.3 public regression has 1,115 assertions. The established
baseline includes three data-dependent note exact-match skips on the
2019-03-19 fixture.

## Full fresh archive cycle

To execute the real archive as part of the comprehensive cycle:

```bat
test\test_everything.bat ..\mvs_dumps_archive --full-archive --workers 8
```

The full mode explicitly uses `--no-cache`. This is intentional: a performance
regression must not be hidden by a cached result from an earlier fast worker.

After execution it runs `check_archive_sweep_quality.bat` against the new
results folder. The interactive HTML report is generated by the sweep.

## Strict performance gate

By default performance outliers are warnings so that a new future dump can be
inspected rather than automatically breaking the functional acceptance result.

Promote outliers to failures with:

```bat
test\test_everything.bat ..\mvs_dumps_archive --strict-performance
```

or:

```bat
test\check_archive_sweep_quality.bat results-folder --strict-performance
```

Thresholds are intended as regression/outlier detectors, not hardware
benchmarks.

## Existing archive results

Analyze a previously completed run during the comprehensive cycle:

```bat
test\test_everything.bat ..\mvs_dumps_archive --archive-results C:\path\archive-sweep-results-...
```

## Public-tool performance output

`test\analyze_test_performance.bat test-results-folder` reads the timed
`all-results.tsv` produced by the generated public regression harness and
writes:

```text
performance-analysis\
    all-timed-cases.tsv
    performance-by-scope.tsv
    performance-by-tool.tsv
    performance-outliers.tsv
    summary.txt
```

The tool table aggregates calls, total time, median, p95, and maximum elapsed
time so the largest public-tool costs can be optimized and the identical test
matrix rerun afterward.

## Archive quality/performance output

`test\check_archive_sweep_quality.bat results-folder` verifies:

- plan and run row counts;
- unique and complete plan indices;
- run/plan metadata identity;
- zero logical `FAIL` rows;
- summary counters against authoritative `runs.tsv`;
- every `SOURCE_MISSING` classification against frozen `snapshots.tsv`
  availability;
- required fast evolution outputs and summary row counts;
- note observation-count bounds and retained raw HTML;
- per-dump retention accounting;
- quality flags;
- per-logical-tool elapsed-time outliers;
- full snapshot-batch elapsed-time outliers.

It writes:

```text
quality-check\
    summary.txt
    performance-by-tool.tsv
    performance-outliers.tsv
    performance-by-batch.tsv
    performance-batch-outliers.tsv
    unexpected-source-missing.tsv   (only when needed)
```

## Fast synthetic acceptance

`test\test_fast_archive_sweep.bat` has no prerequisite. Its three-snapshot
fixture deliberately includes:

- an ordinary layout;
- a nested `mvs_dmp` layout;
- case/whitespace/ID normalization changes;
- legacy `<h3>Title [ID: ...]</h3>` notes;
- newer `<h1>Title</h1>` notes;
- one unchanged note across all three snapshots;
- a one-snapshot note;
- changed product/variant membership.

The test asserts 1,306 logical checks, exact fixed history/all-ever trees,
4 all-ever product states, 4 all-ever variant states, 5 note versions,
5 note bodies, five raw-note variants, and a three-snapshot observation count
for the unchanged Keep note.

## Optimization loop

A normal performance-development cycle is:

```text
test
  -> inspect performance-by-tool / performance-by-batch
  -> optimize maintained source
  -> regenerate standalone artifacts
  -> rerun identical functional tests
  -> rerun fresh performance test
  -> require no functional regression and review performance delta
```

Cached archive runs are useful for frequent catalog validation. Fresh
`--no-cache` runs are the authoritative performance measurement.

## Product-family feature acceptance (0.15.0)

`test\test_product_family_tools.bat` 0.1.4 is an archive-level synthetic regression
and does not require a real dump argument. It builds the family index from a
three-snapshot fixture, including one nested `mvs_dmp` layout, then performs 106
assertions covering normalized output files, hierarchy semantics, false-positive
guards, exact-title overrides, content-addressed raw notes, every positive
query wrapper, and every no-result query wrapper. In 0.19.2 those wrappers are resolved from `tools\`, matching the delivered layout.

The normal `test_all.bat` suite invokes that test once and records one aggregate
family assertion. `test_everything.bat` therefore exercises the family feature
through its normal `test_all.bat` stage.

The archive sweep deliberately excludes the 34 family tools from per-snapshot
planning; their semantics require one archive-level index. The established
legacy plan remains 34,822 logical checks on the known 79-snapshot archive.

## Project 0.19.3 structure regression correction

Structure scope is 502 assertions and all mode is 1,115 assertions. The two
0.19.2 false negatives for timestamped root HTML and moved product-family query
tools now compare literal source markers without PowerShell variable
interpolation. A new structure assertion verifies that early pipeline test-gate
failures preserve test-result evidence and explicitly report downstream archive
work as NOT RUN.

## Project 0.20.0 adaptive-worker/progress regression

Structure scope is 505 assertions and all mode is 1,118 assertions. Three new
guards require: (1) adaptive snapshot workers with start/max controls plus
CPU/memory/physical-disk headroom and throughput checks; (2) concise
`current/total` progress without redundant `remaining=` counters; and (3) a
semantic maintenance fingerprint that excludes scheduler-only changes while
allowing only the explicitly accepted 0.19.2/0.19.3 fingerprint migration.

On the established representative real dump, the same three note-dependent
exact lookups may SKIP, so the expected clean result is 1,115 PASS / 0 FAIL /
3 SKIP.

