MVS Explorer Toolkit Project Version History

0.14.1 - 2026-08-30
  - Maintenance release for archive sweep metadata serialization on Windows PowerShell 5.1.
  - Kept all 443 public root tools byte-for-byte identical to 0.14.0 / supplied 0.13.3 baseline.
  - Fixed summary.txt and run-info.txt array entries so labels and values remain on one physical line.
  - Kept test_fast_archive_sweep.bat strict and added run-info executor assertions.
  - Added a static validator guard rejecting the PowerShell-5.1-sensitive unparenthesized form.
  - Recorded 0.14.0 Windows public regression: 1053 pass / 0 fail / 3 skip.
  - Recorded that the 0.14.0 fast executor itself completed 1306/1306 logical checks with FAIL=0 before the metadata assertion failed.
  - Added a formal development handoff and expanded user design directives covering uninterrupted full acceptance, recurring performance optimization, non-destructive bad-dump handling, future dumps, note preservation, and HTML sanity checking.

0.14.0 - 2026-08-30
  - Kept all 443 public root tools byte-for-byte identical to the supplied 0.13.3 baseline.
  - Replaced repeated fast-worker relationship/detail scans with snapshot-local indexes.
  - Added bounded parallel snapshot workers (--workers) and content-addressed snapshot-result caching.
  - Added source-inventory freeze/recheck so mid-batch moves or edits invalidate the entire batch.
  - Added per-dump first-seen contributions and introduced-here/seen-later retention analysis.
  - Added duplicate/quality metrics, variant source-ID regime transitions, and advisory exclusion suggestions.
  - Added non-destructive canonical exclusions; excluded dumps remain fully ingested as evidence.
  - Added versioned note history with legacy h3+ID and newer h1 parsing, raw HTML retention, and provenance.
  - Added self-contained interactive archive-summary.html.
  - Added test\test_everything.bat, public-test performance analysis, archive quality checks, and strict performance gates.
  - Added logical-tool and whole-snapshot batch performance outlier reports.
  - Strengthened the three-snapshot fast acceptance fixture with concrete product/variant/note all-ever assertions.
  - Fixed fast archive PASS summary accounting and reduced archive parsing callback overhead.

0.13.3 - 2026-08-29
  - Added one-pass fast archive history/all-ever worker.
  - Fast-combined mode no longer runs the two literal archive builders.
  - Preserved all 443 public tool interfaces unchanged.
  - Added per-snapshot archive-worker progress and incremental history ledger output.
  - Preserved --external-tools as the literal public-builder compatibility path.
  - Preserved the 34,822-row fast-combined plan identity for resume compatibility.

0.13.2 - 2026-08-28
  - Maintenance release for test_fast_archive_sweep.bat metadata validation.
  - 443 public root tools are byte-for-byte unchanged from 0.13.1.
  - No prerequisite test_all.bat run is required before test_fast_archive_sweep.bat.
  - Replaced brittle FINDSTR exact-line metadata assertions with PowerShell UTF-8 content checks.
  - Fast acceptance-test failures now print and retain the temporary results folder, summary, run-info, and captured sweep log.
  - Added dev\templates\fast-archive-test.bat.tpl so the acceptance test is generated from maintained source.
  - Added a static regression guard rejecting the old FINDSTR executor metadata check.

0.13.1 - 2026-08-28
  - Maintenance release for the fast-combined archive sweep.
  - 443 public root tools are unchanged from 0.13.0.
  - Fixed Windows PowerShell 5.1 parse failure in fast snapshot Test-HashResult.
  - Replaced the parser-sensitive nested hash/filename predicate with explicit branch-based logic.
  - Added a static regression guard rejecting the old parser-sensitive predicate form.
  - Recorded clean 0.13.0 public-tool regression: 1053 pass / 0 fail / 3 skip in 00:06:18.

0.13.0 - 2026-08-28
  - Added dual-executor archive sweep architecture.
  - test\test_all_dumps.bat now defaults to fast-combined execution.
  - Added --external-tools to force literal public-tool execution.
  - Added test\fast\run_snapshot_tools_fast.bat and run_compare_tools_fast.bat.
  - Added test\analyze_archive_sweep_performance.bat and test\test_fast_archive_sweep.bat.
  - Public root tool count remains 443.
  - Performance-only regenerated 377 existing public tools with unchanged interfaces/scopes:
    120 scalar, 7 lookup, 96 relationship, and 154 single-dump tools.
  - The remaining 66 public tools are byte-for-byte unchanged from 0.12.0.
  - Summary/statistics uses HashSet-backed membership instead of repeated array scans.
  - Single-dump and relationship runtimes avoid parsing/enrichment that the requested operation does not need.
  - external-public sweep mode discards successful stdout instead of writing temporary payload files.
  - Executor identity is part of plan.tsv/plan SHA-256 and resume safety.
  - Combined status logic replayed exactly against 1,171 completed 0.12.0 real Windows sweep rows.
  - Added performance architecture documentation and static archive-sweep validation.

0.12.0 - 2026-08-27
  - Recorded the clean 0.11.0 Windows baseline: 1053 pass / 0 fail / 3 skip.
  - Added test\test_all_dumps.bat for archive-wide real-snapshot execution.
  - Public root tool count remains 443.
  - Discovers all mvs_* snapshots and resolves ordinary or nested mvs_dmp source roots.
  - Runs 422 single-snapshot public tools against every snapshot.
  - Runs all 19 compare tools across every adjacent snapshot transition.
  - Runs change-history then all-ever builders once against the archive.
  - For the supplied 79-snapshot archive the deterministic plan is 34,822 invocations.
  - Derives representative product/date/note/variant/file/hash query values per snapshot.
  - Records PASS, NO_RESULT, SOURCE_MISSING, and FAIL separately.
  - Retains full stdout/stderr/meta only for actual failures to limit result volume.
  - Retains archive builder outputs under archive-output.
  - Added deterministic plan.tsv plus plan SHA-256 and safe --resume support.
  - Added --plan-only mode for preflight inspection without launching public tools.
  - Added maintained archive-sweep library/template/generator and archive-sweep documentation.

0.11.0 - 2026-08-27
  - Recorded the clean 0.10.0 Windows baseline: 986 pass / 0 fail / 3 skip.
  - Recorded the dedicated 0.10.0 compare regression: 39 pass / 0 fail / 0 skip.
  - Added build_mvs_dump_change_history.bat.
  - Added build_mvs_dump_all_ever.bat.
  - Reused the 19 source-local comparison domains and exact normalization policy.
  - Added separate added/removed TSV ledgers with adjacent transition provenance.
  - Added explicit source-coverage tracking; missing sources are not treated as empty sets.
  - Resolves source files at snapshot root or nested snapshot\mvs_dmp\ for known archive layouts.
  - Added monotonic all-ever unions seeded from the first available source snapshot.
  - All-ever rows retain first seen, last seen, observation count, and display value.
  - Added three-snapshot synthetic history fixture and independent fixed expected outputs.
  - Added test\test_history_tools.bat and integrated history checks into test_all.
  - Bumped generated test harness metadata to 0.8.0.
  - Increased standalone public root tool count from 441 to 443.

0.10.0 - 2026-08-27
  - Recorded the clean attached 0.9.2 Windows baseline on mvs_2020-04-21:
    928 passed, 0 failed, 3 data-dependent note skips (931 total).
  - Added 19 standalone two-dump comparison tools.
  - Added ID comparisons for mvs.txt, mvs_ids.txt, mvs_names.txt, and
    mvs_dates.txt; the repeated requested mvs_names entry was implemented once
    and the natural mvs_dates form completes the source matrix.
  - Added title comparisons for mvs.txt, mvs_ids.txt, mvs_names.txt, and
    mvs_dates.txt.
  - Added requested date comparison for mvs_dates.txt.
  - Added SHA-1 comparisons for mvs.txt, mvs_names.txt, and mvs.sha1.
  - Added SHA-256 comparisons for mvs.txt, mvs_names.txt, and mvs.sha256.
  - Added filename comparisons for mvs.txt, mvs_names.txt, mvs.sha1, and
    mvs.sha256.
  - Comparison output is removals first (-), then additions (+), with source
    order preserved on each side and duplicate occurrences collapsed.
  - Interactive console removals are red and additions green; redirected
    stdout is plain text for tests/pipelines.
  - Added synthetic before/after comparison fixture, 19 fixed independent
    expected-output files, and test_compare_tools.bat.
  - Added 39 comparison assertions (19 diff + 19 no-change + fixture check).
  - Public standalone tool count is now 441; full suite is now 989 assertions.
  - Existing 422 public tools remain unchanged.

0.9.2 - 2026-08-27
  - Analyzed the supplied Windows 0.9.1 full run on mvs_2019-10-16:
    762 passed, 166 failed, 3 data-dependent note skips.
  - Confirmed the 0.9.0 PowerShell parse-time defect is fixed: all new tools
    now compile, but all 166 single-dump behavioral checks reached the same
    runtime null-method failure.
  - Identified PowerShell collection enumeration in New-ArrayList: returning
    a bare empty ArrayList emitted no object and assigned $null to callers.
  - Changed New-ArrayList to return the collection as one object with unary
    comma.
  - Regenerated all 154 single-dump tools as version 0.1.2.
  - Added static and test-harness structure guards for the non-enumerating
    ArrayList return.
  - Bumped the seven test entry points to 0.6.2.
  - Public standalone tool count remains 422; full suite remains 931
    assertions.

0.9.1 - 2026-08-27
  - Analyzed two supplied Windows 0.9.0 runs.
  - Confirmed all 166 completed-run single-dump failures share one
    ScriptBlock.Create parser error on Windows PowerShell 5.1.
  - Replaced the parser-sensitive raw-section field array with the proven
    ArrayList-plus-TSV emitter pattern.
  - Bumped all 154 single-dump public tools from 0.1.0 to 0.1.1.
  - Added a static validator regression guard for the unsafe source form.
  - Corrected the data model: mvs_names.txt IDs are source-level IDs, not
    established product IDs or product foreign keys.
  - Corrected mvs_names.txt parsing to preserve numeric, alphanumeric, and
    hyphenated source IDs; variant ID lookup is textual/case-insensitive.
  - Updated the synthetic variant fixture to use a separate alphanumeric ID
    domain so product-ID assumptions cannot regress.
  - Renamed misleading variant summary metrics to source-ID metrics.
  - Added reference summary output for mvs_2019-10-16 from the independent
    Python parser.
  - Public standalone tool count remains 422.
  - Full test-suite assertion count remains 931.

0.9.0 - 2026-08-27
  - Recorded the clean attached 0.8.0 Windows baseline: 607 passed,
    0 failed, 3 data-dependent note skips.
  - Added 154 standalone single-dump completeness tools.
  - Added forward product ID/title -> filename -> hash traversal.
  - Added first-class mvs_names.txt variant occurrence presentation.
  - Added hash provenance enumeration/query across mvs.txt, mvs_names.txt,
    mvs.sha1, and mvs.sha256.
  - Added product-file and raw product-section presentation.
  - Added raw note occurrence presentation.
  - Added 12 unparsed-line finder/reader tools across six sources.
  - Added 10 hash-integrity diagnostic tools.
  - Added print/read summary and statistics tools.
  - Added comprehensive synthetic single-dump fixture and 154 fixed
    independent expected-output files.
  - Added test_single_dump_tools.bat with 167 assertions.
  - Full 0.9.0 suite expects 931 assertions before real-dump-dependent skips.
  - Root public standalone tool count is now 422.

0.8.0 - 2026-08-27
  - Recorded the clean attached 0.7.0 Windows baseline: 363 passed,
    0 failed, 0 skipped against mvs_2020-10-20.
  - Added 96 standalone filename/hash relationship query tools.
  - Added 24 unique requested projections, each as print/read from filename
    and print/read from hash.
  - Filename/hash matching is exact and case-insensitive.
  - Hash reverse lookup indexes mvs.txt, mvs.sha1, and mvs.sha256.
  - mvs.txt is the canonical product-to-filename ownership source.
  - Preserves one-to-many hash->filename and filename->product associations.
  - Adds date-by-ID and established title-level note joins.
  - Added synthetic test\test-mvs-dump-relationships fixture.
  - Added 146 fixed independent relationship behavior expectations.
  - Added test\test_relationship_tools.bat and relationship result scope.
  - Hash tools are regression-tested with manifest-only SHA-1 and SHA-256;
    additional cases prove direct mvs.txt hash discovery.
  - Full 0.8.0 test matrix expects 610 assertions.
  - Root public standalone tool count is now 268.
  - Added relationship docs, matrices, development generators, histories,
    directives, style guidance, diary, observations, and 0.7.0 test analysis.

0.7.0 - 2026-08-27
  - Recorded the clean attached 0.6.0 Windows baseline: 272 passed,
    0 failed, 0 skipped against mvs_2020-09-15_2.
  - Added 45 standalone duplicate/orphan diagnostic finders.
  - Added 14 duplicate finders (13 requested forms plus canonical plural
    alias for the requested singular mvs_date.txt tool name).
  - Added 31 directional orphan finders exactly covering the requested
    ID/title/filename source-target comparisons.
  - Duplicate/orphan section findings preserve full mvs.txt/mvs_names.txt
    source sections as appropriate.
  - Filename findings include owning title/ID and matched filename line.
  - Added explicit documentation that repeated IDs in mvs_names.txt are
    normally expected variant relationships.
  - Added synthetic test\test-mvs-dump-diagnostics fixture.
  - Added 45 fixed independently generated expected diagnostic outputs.
  - Added test\test_diagnostic_tools.bat and integrated diagnostics into
    test\test_all.bat.
  - Root public standalone tool count is now 172.
  - Added diagnostic tools/fixture documentation, development generators,
    histories, directives, style guidance, diary, and observations.

0.6.0 - 2026-08-27
  - Reviewed the attached 0.5.0 Windows timestamped result bundle.
  - Confirmed 128 structure passes, 120 scalar passes, 17 lookup passes,
    and the same 7 lookup no-match return-code failures.
  - Confirmed all seven failure stdout/expected/stderr streams are empty;
    only expected return code 1 versus actual 0 differs.
  - Reworked :RunPowerShellFromLabel 1.2.0 to return the captured
    powershell.exe exit code directly.
  - Removed the _rps_rc re-entry return carrier from that bridge.
  - Added an explicit top-level nonzero exit /b before normal GoTo :EOF.
  - Kept the explicit PowerShell process-level no-match exit.
  - Regenerated all public and test batch files.
  - Added doc\test-run-analysis-0.5.0.md.
  - Updated development directives, style addendum, PowerShell guide,
    diary, observation journal, prompt record, and histories.

0.5.0 - 2026-08-27
  - Analyzed the supplied Windows 0.4.0 run against mvs_2021-01-12-1901:
    265 passed, 7 failed, 0 skipped.
  - Isolated all seven failures to lookup no-match return-code propagation.
  - Fixed lookup no-match to return 1 while keeping stdout/stderr empty.
  - Made documented scalar/lookup fatal nonzero exits explicit at PowerShell
    process level for reliable propagation through the batch bridge.
  - Added timestamped per-invocation test result directories under test\.
  - Added console.log, summary.txt, run-info.txt, all-results.tsv, and
    general/structure/scalar/lookup TSV result files.
  - Added complete expected/actual/stderr/meta files for behavioral failures.
  - Added doc\test-run-analysis-0.4.0.md.
  - Updated project testing/style/development documentation.
  - Regenerated all public and test batch files from maintained source.

0.4.0 - 2026-08-27
  - Added standalone automated Windows test suite under test\.
  - Added test\test_all.bat as the full-suite root entry point.
  - Added standalone structure, scalar, and lookup subset tests.
  - Full suite executes all 127 public batch tools.
  - Scalar tests compare all 120 scalar tools against independently derived
    expected output from the selected dump.
  - Lookup tests cover all 7 lookup tools, exact/wildcard/no-match behavior,
    and explicit prefix/suffix/contains ID wildcard cases.
  - Tests capture/validate stdout, stderr, and return codes.
  - Added dev\generate_tests.py and injected test harness source/template.
  - Updated testing directives, style addendum, PowerShell guide, diary,
    observation journal, prompt record, usage, and README.
  - Replaced DateTimeOffset.UtcTicks with UtcDateTime.Ticks in maintained
    PowerShell sort helpers for safer Windows PowerShell/.NET compatibility.
  - Regenerated all public tools from maintained development source.

0.3.0 - 2026-08-27
  - Added shared development-time libraries/templates and generator while
    preserving fully standalone public batch deliverables.
  - Regenerated 30 source-order scalar tools from injected common source.
  - Added 90 sorted scalar tools: every scalar print/read projection now has
    sorted_by_id, sorted_by_title, and sorted_by_date companions.
  - ID sort is numeric ascending.
  - Title sort is case-insensitive natural/alphanumeric with ID tie-break.
  - Date sort is chronological where parseable with deterministic tie-breaks.
  - Added 7 lookup tools.
  - Added exact and '*' wildcard lookup matching.
  - Multiple lookup matches are ordered by searched/source field.
  - Lookup output is distinct non-empty associated values, one per line.
  - Added development generator, validator, templates, common source, and
    tool specification.
  - Updated project documentation and development records.

0.2.0 - 2026-08-27
  - Reworked all scalar public batch files to be fully standalone.
  - Added development documentation, directives, histories, style addendum,
    embedded PowerShell guide, and observation journal.

0.1.0 - 2026-08-27
  - Initial scalar-product toolkit using a shared runtime parser.
