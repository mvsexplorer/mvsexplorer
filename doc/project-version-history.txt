MVS Explorer Toolkit Project Version History

0.7.0 - 2026-08-27
  - Recorded the clean attached 0.6.0 Windows baseline: 272 passed,
    0 failed, 0 skipped against mvs_2020-09-15_2.
  - Added 45 standalone duplicate/orphan diagnostic finders.
  - Added 14 duplicate finders (13 requested forms plus canonical plural
    alias for the requested singular mvs_date.txt tool name).
  - Added 31 directional orphan finders exactly covering the requested
    ID/title/filename source-target comparisons.
  - Duplicate/orphan section findings preserve full mvs.txt/mvs_names.txt
    source sections as appropriate.
  - Filename findings include owning title/ID and matched filename line.
  - Added explicit documentation that repeated IDs in mvs_names.txt are
    normally expected variant relationships.
  - Added synthetic test\test-mvs-dump-diagnostics fixture.
  - Added 45 fixed independently generated expected diagnostic outputs.
  - Added test\test_diagnostic_tools.bat and integrated diagnostics into
    test\test_all.bat.
  - Root public standalone tool count is now 172.
  - Added diagnostic tools/fixture documentation, development generators,
    histories, directives, style guidance, diary, and observations.

0.6.0 - 2026-08-27
  - Reviewed the attached 0.5.0 Windows timestamped result bundle.
  - Confirmed 128 structure passes, 120 scalar passes, 17 lookup passes,
    and the same 7 lookup no-match return-code failures.
  - Confirmed all seven failure stdout/expected/stderr streams are empty;
    only expected return code 1 versus actual 0 differs.
  - Reworked :RunPowerShellFromLabel 1.2.0 to return the captured
    powershell.exe exit code directly.
  - Removed the _rps_rc re-entry return carrier from that bridge.
  - Added an explicit top-level nonzero exit /b before normal GoTo :EOF.
  - Kept the explicit PowerShell process-level no-match exit.
  - Regenerated all public and test batch files.
  - Added doc\test-run-analysis-0.5.0.md.
  - Updated development directives, style addendum, PowerShell guide,
    diary, observation journal, prompt record, and histories.

0.5.0 - 2026-08-27
  - Analyzed the supplied Windows 0.4.0 run against mvs_2021-01-12-1901:
    265 passed, 7 failed, 0 skipped.
  - Isolated all seven failures to lookup no-match return-code propagation.
  - Fixed lookup no-match to return 1 while keeping stdout/stderr empty.
  - Made documented scalar/lookup fatal nonzero exits explicit at PowerShell
    process level for reliable propagation through the batch bridge.
  - Added timestamped per-invocation test result directories under test\.
  - Added console.log, summary.txt, run-info.txt, all-results.tsv, and
    general/structure/scalar/lookup TSV result files.
  - Added complete expected/actual/stderr/meta files for behavioral failures.
  - Added doc\test-run-analysis-0.4.0.md.
  - Updated project testing/style/development documentation.
  - Regenerated all public and test batch files from maintained source.

0.4.0 - 2026-08-27
  - Added standalone automated Windows test suite under test\.
  - Added test\test_all.bat as the full-suite root entry point.
  - Added standalone structure, scalar, and lookup subset tests.
  - Full suite executes all 127 public batch tools.
  - Scalar tests compare all 120 scalar tools against independently derived
    expected output from the selected dump.
  - Lookup tests cover all 7 lookup tools, exact/wildcard/no-match behavior,
    and explicit prefix/suffix/contains ID wildcard cases.
  - Tests capture/validate stdout, stderr, and return codes.
  - Added dev\generate_tests.py and injected test harness source/template.
  - Updated testing directives, style addendum, PowerShell guide, diary,
    observation journal, prompt record, usage, and README.
  - Replaced DateTimeOffset.UtcTicks with UtcDateTime.Ticks in maintained
    PowerShell sort helpers for safer Windows PowerShell/.NET compatibility.
  - Regenerated all public tools from maintained development source.

0.3.0 - 2026-08-27
  - Added shared development-time libraries/templates and generator while
    preserving fully standalone public batch deliverables.
  - Regenerated 30 source-order scalar tools from injected common source.
  - Added 90 sorted scalar tools: every scalar print/read projection now has
    sorted_by_id, sorted_by_title, and sorted_by_date companions.
  - ID sort is numeric ascending.
  - Title sort is case-insensitive natural/alphanumeric with ID tie-break.
  - Date sort is chronological where parseable with deterministic tie-breaks.
  - Added 7 lookup tools.
  - Added exact and '*' wildcard lookup matching.
  - Multiple lookup matches are ordered by searched/source field.
  - Lookup output is distinct non-empty associated values, one per line.
  - Added development generator, validator, templates, common source, and
    tool specification.
  - Updated project documentation and development records.

0.2.0 - 2026-08-27
  - Reworked all scalar public batch files to be fully standalone.
  - Added development documentation, directives, histories, style addendum,
    embedded PowerShell guide, and observation journal.

0.1.0 - 2026-08-27
  - Initial scalar-product toolkit using a shared runtime parser.
