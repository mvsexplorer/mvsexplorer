0.2.0 / project 0.21.2 - 2026-09-15
- Aligns HTML hierarchy controls with the Explorer interaction contract: Select all, cascading Clear, Copy selected, Copy all, and Name/Count/Reverse ordering.
- Variant / exact-title badges count applicable product-backed Files & hashes rows.
- Products and Files & hashes tables sort by any clicked column.
- Complete-table and per-column clipboard export uses all filtered/sorted rows across every page.
- Adds selectable 50/100/200/500/1000/All page sizes.
- Keeps the browser self-contained/offline and preserves compact-database evidence semantics.

0.1.2 / project 0.19.0 - 2026-09-07
- Delivery path moves to `tools\build_mvs_html_browser.bat`.
- When output is omitted, writes a date/time-stamped `mvs-browser-YYYYMMDD-HHmmss.html` in the project root.
- Explicit output paths and browser data/interaction semantics are unchanged.

0.1.1 / project 0.17.1 - 2026-09-07
- Fixes Windows PowerShell 5.1 parsing of ordinal string sorting.
- Replaces `[Array]::Sort[string](...)` with the PS5.1-compatible non-generic overload.
- No browser hierarchy, payload, filtering, evidence or output-format change.

0.1.0 / project 0.17.0 - 2026-09-07
- Initial standalone self-contained HTML browser builder.
- Consumes an existing compact product-family index and emits one offline HTML file.
- Cascading multi-select hierarchy: broad family, product family, release, exact historical product title.
- Includes product metadata, IDs/dates, product-backed filenames/hashes, notes, raw-note evidence hashes, and observed ranges.
- Preserves explicit unclassified/historical titles and the existing conservative evidence semantics.
- Dictionary-encodes repeated browser payload strings and pages large detail tables.
