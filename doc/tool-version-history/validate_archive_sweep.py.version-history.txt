validate_archive_sweep.py Version History

0.6.1 - 2026-09-01
  - Guards the 0.15.1 release-token hardening and its new real-title-shaped family regression cases.
  - Requires the aggregate test harness to expect the exact 96-assertion family summary and rejects the stale 92-case success signature.

0.6.0 - 2026-09-01
  - Validates the separate 33-tool product-family class and its 92-assertion synthetic regression.
  - Preserves static 34,822/1,306 legacy archive plan invariants while the root public surface grows to 476.

0.5.2 - 2026-08-30
  - Requires streamed fast archive progress instead of stdout suppression.
  - Guards the archive-worker hot-path optimizations and archive-scope performance outlier report.

0.5.1 - 2026-08-30
  - Requires extensionless fast runs.tsv tool-key assertions.
  - Requires legacy h1/h3 note profiling and legacy ID-suffix stripping.
  - Rejects test_everything Run helpers that collide with PowerShell's automatic $args variable.

0.3.1 - 2026-08-30
  - Rejects unparenthesized archive summary/run-info concatenation that splits metadata fields under Windows PowerShell 5.1.
  - Requires fast acceptance assertions for executor metadata in both summary.txt and run-info.txt.

0.5.0 - 2026-08-30
  - Added case-insensitive duplicate batch-label validation across delivered batch files.
  - Requires indexed fast-worker markers, source-stability/cache features, note h3/h1 acceptance, retention, quality, HTML, and performance artifacts.
  - Requires concrete 4/4 product/variant and 5/5/5 note synthetic all-ever assertions.

0.2.1 - 2026-08-28
  - Rejects the PowerShell 5.1-sensitive nested hash predicate in fast workers.
  - Requires the explicit branch-based hash predicate markers.

0.2.0 - 2026-08-28
  - Static validation for dual-executor archive sweep, fast utilities, tool counts, and plan counts.