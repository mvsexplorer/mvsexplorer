Project 0.19.0 layout note - 2026-09-07
- Root full-pipeline entry point is retained.
- Utility tool resolution follows the 0.19.0 tools\ layout; production semantics are otherwise unchanged.

1.0.3 - 2026-09-07
- Database/log ZIP creation now normalizes entry timestamps to 1980-01-01 UTC so validation wall-clock dates cannot cause checksum drift.
- File names/bytes and sorted entry order remain authoritative; raw evidence content hashes are unchanged.

1.0.4 / project 0.16.4
- Defers log ZIP creation and the SEND-ME-LOGS hardlink until after console.log and phase-performance.tsv writers are flushed and disposed.
- Keeps the database ZIPs/hardlinks in the final collection phase and preserves failure-log recovery.
- Retains token-level semantic console coloring, the 57-check real database gate, and all database formats unchanged.

1.0.3 / project 0.16.3
- Fixes active-log ZIP packaging by opening console.log and phase-performance.tsv with explicit FileShare.ReadWrite.
- Replaces whole-line status coloring with token-level semantic coloring while preserving plain-text retained logs.
- Keeps resume mode, the 57-check real database gate, and all database formats unchanged.

1.0.2 / project 0.16.2
- Colorizes live PASS output green, FAIL/error output red, and warning/SKIP output yellow on interactive consoles while keeping retained logs plain text.
- Retains 0.16.1 resume mode and the ten-phase clean production workflow.
- Works with the corrected 0.1.2 generated-database validator and concise phase-1 plan preflight.

1.0.1 / project 0.16.1
- Fixes Windows PowerShell 5.1 return-token boundary failures in summary/log helpers.
- Adds --resume-built ARCHIVE_DB FAMILY_DB COMPACT_DB and optional --resume-test-results DIR.
- Resume mode first runs the structure-only preflight, then revalidates archive/family/compact databases, runs all 32 family queries, and performs normal ZIP/log/hardlink packaging without rebuilding.

1.0.0
- Initial one-command production pipeline for project 0.16.0.
- Fail-gates all database generation on test_everything success.
- Builds archive, full family and compact family databases; validates them; runs all family queries; packages databases/logs and creates SEND-ME hardlinks.
- Records ten phase timings and final paths/SHA-256 metadata.
