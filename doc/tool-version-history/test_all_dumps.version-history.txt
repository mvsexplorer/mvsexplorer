0.4.6 - 2026-09-06
  - Replaces banner-only snapshot/compare output with paired Starting/Completed lines.
  - Completion lines include elapsed seconds and the cumulative logical status counters.
  - Adds --quiet-plan to suppress per-snapshot planning chatter during automated preflight.

test_all_dumps.bat Version History

0.4.3 - 2026-08-30
  - Streams fast archive worker progress through Write-Line instead of discarding stdout.
  - Retains per-snapshot fast archive timing lines in console.log while preserving status-return semantics.
  - Keeps the 34,822-row logical plan and public-tool interfaces unchanged.

0.4.2 - 2026-08-30
  - Snapshot profiling now recognizes both h1 and historical h3 note headings.
  - Legacy [ID: ...] suffixes are removed before note-title linkage/search selection.
  - Preserves the existing public-tool plan and executor interfaces.

0.4.1 - 2026-08-30
  - Parenthesized archive summary/run-info metadata concatenations for Windows PowerShell 5.1.
  - Preserved executor identity and logical plan behavior.

0.4.0 - 2026-08-30
  - Added indexed snapshot execution and bounded --workers parallelism.
  - Added source-inventory freeze/recheck before logical batch commit.
  - Added content-addressed snapshot result cache, --cache-folder, and --no-cache.
  - Added non-destructive --exclusions input and automatic interactive HTML reporting.
  - Fast archive completion now contributes both logical PASS rows to summary counters.

0.2.0 - 2026-08-28
  - Default executor changed to fast-combined for high-throughput archive validation.
  - Added --external-tools for literal public-tool execution.
  - Executor identity is serialized into the deterministic plan and resume hash.
  - external-public mode discards successful stdout and captures full payload only on failure.
  - Fast batches retain the same 34,822 logical-check plan for the supplied 79-snapshot archive.

0.1.0 - 2026-08-27
  - Initial archive-wide real-dump execution harness.
  - Discovers mvs_* snapshots and resolves nested mvs_dmp source roots.
  - Executes all single-snapshot public tools against every snapshot.
  - Executes compare tools on every adjacent snapshot pair.
  - Executes history/all-ever builders once at archive scope.
  - Derives representative query arguments from each snapshot.
  - Adds deterministic plan, plan SHA-256, plan-only, and safe resume.
  - Records PASS, NO_RESULT, SOURCE_MISSING, and FAIL separately.
  - Retains full failure artifacts and archive-builder datasets.